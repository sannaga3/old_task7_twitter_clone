<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>Whisper_edit</title>
    <link href="/assets/normalize.css" rel="stylesheet">
    <link href="/assets/new.scss" rel="stylesheet">
  </head>
  <body>
    <div class="contents_wrapper">
      <header>
        <%= render 'header' %>
      </header>
      <div>
        <%= form_with(model: @post, local: true ,url: confirm_posts_path, action: :post) do |form| %>
          <% if @post.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
              <ul>
                <% @post.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          <div class="whisper_wrapper">
            <div class="field">
              <%= form.label :'内容' %>
              <%= form.text_area :content %>
            </div> 
            <div class="whisper_button">
              <%= form.submit value="内容確認" %>
            </div>
          </div>
        <% end %>
        <footer>
          <%= render 'footer' %>
        </footer>
      </div>
    </div>
  </body>
</html>